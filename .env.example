# Environment variables for append-review-v1
# Copy this file to .env and adjust as needed. Never commit the real .env.

# Server port (default 5252 if unset)
PORT=5252
# Preferred host interface (default 127.0.0.1). Set to 0.0.0.0 to listen on all interfaces.
HOST=127.0.0.1
# Alternate variable name supported by the server for host binding
# BIND_HOST=

# Base path for serving the built client (used during build commands)
# VITE_BASE_PATH=./

# Set NODE_ENV=production in deployment environments
# NODE_ENV=development
